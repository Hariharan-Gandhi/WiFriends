<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeightLarge"
    android:background="@color/colorPrimaryLightHelp"
    android:padding="6dip">

    <ImageView
        android:id="@+id/status_profile_pic"
        android:layout_width="?android:attr/listPreferredItemHeightLarge"
        android:layout_height="fill_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="6dip"
        android:background="@color/fontColorProfilePage"
        android:contentDescription="ProfilePic"
        android:scaleType="fitXY"
        android:src="@drawable/ic_default_profile_pic" />

    <TextView
        android:id="@+id/userStatusHelper"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/userStatus"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@id/status_profile_pic"
        android:ellipsize="marquee"
        android:text="Description"
        android:textColor="@color/background"
        android:textSize="12sp" />


    <TextView
        android:id="@+id/userStatus"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toEndOf="@id/status_profile_pic"
        android:gravity="center_vertical"
        android:text="WF User Name"
        android:textColor="@color/background"
        android:textSize="16sp" />

</RelativeLayout>